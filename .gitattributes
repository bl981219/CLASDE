# Normalize all text files to LF on commit (prevents CRLF noise from OneDrive/Windows)
* text=auto eol=lf

# Treat binary assets as binary (no line-ending conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.db binary
